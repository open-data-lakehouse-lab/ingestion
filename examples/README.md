# Examples

This directory contains examples of how to use and configure the ingestion tool.

## Local Ingestion Example

To run a placeholder ingestion locally:

```bash
odl-ingestion ingest --dataset meteocat-weather --target local --output-dir ./data
```

## Configuration Files

See `configs/local.example.yml` for a non-secret configuration example.
